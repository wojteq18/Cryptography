[package]
name = "App"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.7", features = ["mysql", "runtime-tokio-native-tls"] }
dotenvy = "0.15"
tokio = { version = "1", features = ["full"] }

